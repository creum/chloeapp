<%= form_for(@chlo) do |f| %>
  <% if @chlo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chlo.errors.count, "error") %> prohibited this chlo from being saved:</h2>

      <ul>
      <% @chlo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
